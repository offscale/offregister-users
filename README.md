offregister_users
===============

## Install package

    pip install .

## Example use in offregister JSON

    {
        "module": "offregister-users",
        "kwargs": {
          "add_users": [
            "name": "ubuntu",
            "groups": null
          ]
        }
    }
